[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    integration: hits external APIs (skipped without credentials)
filterwarnings =
    ignore::DeprecationWarning
